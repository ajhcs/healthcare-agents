# Demo GitHub Copilot Repo Setup

Run this from a repository root:

```bash
healthcare-agents install --copilot-all --dry-run
healthcare-agents install --copilot-all
```

Generated surfaces include `.github/copilot-instructions.md`, path-specific instructions, curated custom agents, prompt files, and issue templates.
