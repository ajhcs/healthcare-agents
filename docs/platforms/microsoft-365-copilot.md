# Microsoft 365 Copilot

Microsoft 365 Copilot support is a governed export path, not a local install. Use `healthcare-agents export m365-declarative-agent <workflow-id>` to review declarative agent instructions, starter prompts, knowledge guidance, action placeholders, tests, and admin checklist.

Do not put hidden system instructions into SharePoint knowledge files. Knowledge sources are knowledge, not instruction bypasses.
