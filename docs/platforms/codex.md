# Codex

Install specialist prompts with `healthcare-agents install --codex` and workflow skills with `healthcare-agents install --codex-skills`.

The installer preserves user content in `~/.codex/AGENTS.md` by using a managed block and follows Codex directory precedence rules.
