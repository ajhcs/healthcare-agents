# Azure AI Foundry

Use `healthcare-agents export azure-foundry <workflow-id>` to produce a prompt/workflow agent spec with tool placeholders, identity, RBAC, observability, versioning, private networking, and Teams/Microsoft 365 distribution notes.

Azure AI Foundry is the enterprise architecture path when organizations need more than prompt files.
