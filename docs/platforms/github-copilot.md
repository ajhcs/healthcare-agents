# GitHub Copilot

Use `healthcare-agents install --copilot-all` inside a repository to generate Copilot repository instructions, path-specific instructions, curated custom agents, prompt files, and issue templates.

VS Code, GitHub.com, JetBrains, Visual Studio, and Copilot coding agent surfaces vary; keep generated files under review and adapt `applyTo` frontmatter for local repo paths.
